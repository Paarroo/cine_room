<% content_for :title, "New event" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New event</h1>

  <%= render "form", event: @event %>

  <%= link_to "Retour aux événements", events_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-600 hover:bg-gray-700 text-white inline-block font-medium transition-colors" %>
</div>
