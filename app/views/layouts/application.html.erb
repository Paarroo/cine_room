<!DOCTYPE html>
<html>
  <head>
    <title>🍿<%= content_for(:title) || "CinéRoom - Cinéma Indépendant" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    
    <%# Conditional analytics and marketing scripts based on cookie consent %>
    <%= render_analytics_scripts %>
    <%= render_marketing_scripts %>

    <%# Icons %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

      <%# FONTS - Dyslexie-friendly fonts %>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
      <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

      <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Font Awesome %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <%# Leaflet CSS %>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" 
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" 
          crossorigin="" />


    <%# CSS - Single application.css file %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "cookie_consent", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "minimap", "data-turbo-track": "reload" %>
    
    <%# Leaflet JS %>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
            integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
            crossorigin=""></script>
    
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-surface text-white min-h-screen flex flex-col">
    <% if params[:controller]&.start_with?('admin/') %>
      <%= render 'admin/dashboard/navbar' %>
    <% else %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%= render 'shared/alerts' %>

    <main class="w-full px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto flex-grow">
    
    
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
    
    <!-- Cookie Consent Banner -->
    <%= render 'shared/cookie_consent' if show_consent_banner? %>
    
    <!-- Cookie consent controller -->
    <div data-controller="cookie-consent"></div>
  </body>
</html>
