<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "CinéRoom - Cinéma Indépendant" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Icons %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# SYLEXIAD FONT - Single font family for jury compliance %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sylexiad:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <%# Font Awesome %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <%# CSS - Single application.css file %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-surface text-content min-h-screen flex flex-col font-sans">
    <%= render 'shared/navbar' %>

    <main class="w-full px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto flex-grow">
      <%# Flash messages - styled with CinéRoom theme %>
      <% if notice %>
        <div class="glass-effect border-l-4 border-primary text-content p-4 mb-6 rounded-xl animate-fade-in">
          <div class="flex items-center">
            <i class="fas fa-check-circle text-primary mr-3"></i>
            <span class="font-medium"><%= notice %></span>
          </div>
        </div>
      <% end %>

      <% if alert %>
        <div class="glass-effect border-l-4 border-red-500 text-content p-4 mb-6 rounded-xl animate-fade-in">
          <div class="flex items-center">
            <i class="fas fa-exclamation-triangle text-red-500 mr-3"></i>
            <span class="font-medium"><%= alert %></span>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
