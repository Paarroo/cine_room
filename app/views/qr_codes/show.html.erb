<div id="qr-code-page" class="page-content active mt-24">
  <section class="relative min-h-screen w-full flex items-center justify-center overflow-hidden">
    
    <!-- Background gradient -->
    <div class="absolute inset-0 bg-gradient-to-br from-dark-400 via-dark-300 to-dark-400"></div>

    <!-- Centered content wrapper -->
    <div class="relative z-10 flex flex-col items-center justify-center text-center w-full px-4 sm:px-6 lg:px-8">
      <div class="mb-8 max-w-5xl mx-auto">
        
        <!-- Top badge -->
        <div class="inline-flex items-center space-x-3 glass-effect rounded-full px-8 py-4 mb-8">
          <div class="w-3 h-3 bg-gold-500 rounded-full animate-pulse"></div>
          <span class="text-gold-400 font-medium text-lg">Billet #<%= @participation.id.to_s.rjust(6, '0') %></span>
        </div>

        <!-- Main title -->
        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-8 leading-tight">
          <span class="gradient-text">QR Code</span>
          <span class="block text-3xl md:text-4xl lg:text-5xl mt-4 text-gray-300">d'Entrée</span>
        </h1>

        <!-- Subtitle -->
        <p class="text-xl md:text-2xl text-gray-300 mb-12 max-w-3xl mx-auto leading-relaxed">
          Présentez ce code à l'entrée de l'événement pour valider votre participation
        </p>

        <!-- QR Code principale -->
        <div class="glass-effect rounded-3xl overflow-hidden mb-12 max-w-lg mx-auto">
          <!-- QR Header -->
          <div class="bg-gradient-to-r from-gold-500 to-gold-600 p-6 text-center">
            <div class="text-2xl font-bold mb-2">🎫 Votre Billet</div>
            <div class="text-white/90">Scannez pour entrer</div>
          </div>
          
          <!-- QR Code -->
          <div class="p-8">
            <div class="bg-white p-6 rounded-2xl mb-6 mx-auto shadow-xl">
              <div class="w-64 h-64 mx-auto">
                <% if @participation.qr_code_token.present? %>
                  <% begin %>
                    <!-- Generate and display QR code SVG with full data -->
                    <% 
                      require 'rqrcode'
                      # Use the full QR code data from the model
                      qrcode = RQRCode::QRCode.new(@participation.qr_code_data)
                      qr_svg = qrcode.as_svg(
                        color: '000',
                        shape_rendering: 'crispEdges',
                        module_size: 3,
                        standalone: true,
                        svg_attributes: {
                          class: 'w-full h-full'
                        }
                      )
                    %>
                    <div class="w-full h-full">
                      <%= raw qr_svg %>
                    </div>
                    <!-- Debug: afficher le token aussi -->
                    <div class="text-xs text-gray-500 mt-2 text-center">
                      Token: <%= @participation.qr_code_token.slice(0, 8) %>...
                    </div>
                  <% rescue => e %>
                    <!-- Fallback si la gem RQRCode n'est pas disponible -->
                    <div class="w-full h-full bg-white rounded-xl flex flex-col items-center justify-center p-4 border border-gray-300">
                      <!-- Debug info -->
                      <div class="text-xs text-red-500 mb-2">Erreur QR: <%= e.message %></div>
                      
                      <!-- QR Code pattern simulé -->
                      <div class="relative w-48 h-48 bg-white border-2 border-black mb-4">
                        <!-- Coins QR caractéristiques -->
                        <div class="absolute top-2 left-2 w-8 h-8 bg-black"></div>
                        <div class="absolute top-2 right-2 w-8 h-8 bg-black"></div>
                        <div class="absolute bottom-2 left-2 w-8 h-8 bg-black"></div>
                        
                        <!-- Pattern central avec token -->
                        <div class="absolute inset-0 flex items-center justify-center">
                          <div class="text-center p-2 bg-white border border-gray-400 rounded">
                            <div class="text-xs font-mono text-black break-all max-w-32">
                              <%= @participation.qr_code_token.slice(0, 16) %>...
                            </div>
                          </div>
                        </div>
                        
                        <!-- Points dispersés pour simuler QR -->
                        <div class="absolute top-4 left-12 w-2 h-2 bg-black"></div>
                        <div class="absolute top-8 left-6 w-2 h-2 bg-black"></div>
                        <div class="absolute top-12 right-8 w-2 h-2 bg-black"></div>
                        <div class="absolute bottom-8 left-16 w-2 h-2 bg-black"></div>
                        <div class="absolute bottom-12 right-6 w-2 h-2 bg-black"></div>
                        <div class="absolute top-6 right-12 w-2 h-2 bg-black"></div>
                        <div class="absolute bottom-6 right-16 w-2 h-2 bg-black"></div>
                      </div>
                      <p class="text-gray-700 font-semibold text-sm">Token d'entrée</p>
                      <p class="text-gray-500 text-xs mt-1">Présentez ce code à l'entrée</p>
                    </div>
                  <% end %>
                <% else %>
                  <div class="w-full h-full bg-gray-200 rounded-xl flex items-center justify-center">
                    <span class="text-gray-500 text-lg">QR Code indisponible</span>
                  </div>
                <% end %>
              </div>
            </div>

            <!-- Status -->
            <% if @participation.used? %>
              <div class="bg-red-500/20 border border-red-500/30 rounded-xl p-4 mb-6">
                <p class="text-red-400 font-semibold text-lg">✅ Billet déjà utilisé</p>
                <p class="text-red-300 text-sm">
                  Entrée validée le <%= I18n.l(@participation.used_at, format: :short) %>
                </p>
              </div>
            <% else %>
              <div class="bg-green-500/20 border border-green-500/30 rounded-xl p-4 mb-6">
                <p class="text-green-400 font-semibold text-lg">🟢 Billet valide</p>
                <p class="text-green-300">Prêt pour l'entrée</p>
              </div>
            <% end %>

            <!-- Download Options -->
            <div class="flex flex-wrap justify-center gap-3">
              <%= link_to participation_qr_code_path(@participation, format: :png),
                  class: "px-4 py-2 bg-gold-500/20 border border-gold-500/30 text-gold-400 rounded-lg hover:bg-gold-500/30 transition-colors flex items-center space-x-2",
                  download: "ticket_qr_#{@participation.id}.png" do %>
                <span>📱</span>
                <span>PNG</span>
              <% end %>
              
              <%= link_to participation_qr_code_path(@participation, format: :svg),
                  class: "px-4 py-2 bg-gold-500/20 border border-gold-500/30 text-gold-400 rounded-lg hover:bg-gold-500/30 transition-colors flex items-center space-x-2",
                  download: "ticket_qr_#{@participation.id}.svg" do %>
                <span>🖼️</span>
                <span>SVG</span>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Détails de l'événement -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 max-w-4xl mx-auto">
          <div class="glass-effect rounded-2xl p-6 text-center">
            <div class="text-3xl mb-3">🎭</div>
            <h3 class="font-bold text-white text-lg mb-2"><%= @participation.event.title %></h3>
            <p class="text-gray-400 text-sm">Film : <%= @participation.event.movie.title %></p>
          </div>
          
          <div class="glass-effect rounded-2xl p-6 text-center">
            <div class="text-3xl mb-3">📅</div>
            <h3 class="font-bold text-white text-lg mb-2">
              <%= I18n.l(@participation.event.event_date, format: :long) %>
            </h3>
            <p class="text-gray-400 text-sm"><%= @participation.event.start_time.strftime('%H:%M') %></p>
          </div>
          
          <div class="glass-effect rounded-2xl p-6 text-center">
            <div class="text-3xl mb-3">📍</div>
            <h3 class="font-bold text-white text-lg mb-2"><%= @participation.event.venue_name %></h3>
            <p class="text-gray-400 text-sm"><%= @participation.event.venue_address %></p>
          </div>
        </div>

        <!-- Actions buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-16">
          <%= link_to participation_ticket_path(@participation), 
              class: "cta-btn group px-8 py-4 bg-gradient-to-r from-gold-500 to-gold-600 hover:from-gold-600 hover:to-gold-700 text-white rounded-full font-semibold text-lg transition-all duration-300 hover:scale-105" do %>
            <i class="fas fa-ticket-alt mr-3 group-hover:rotate-12 transition-transform"></i>
            Voir le billet complet
          <% end %>

          <%= link_to users_dashboard_path(current_user), 
              class: "cta-btn px-8 py-4 bg-gradient-to-r from-cinema-blue to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white rounded-full font-semibold text-lg transition-all duration-300 hover:scale-105" do %>
            <i class="fas fa-user mr-3"></i>
            Mon tableau de bord
          <% end %>
        </div>

        <!-- Participant info -->
        <div class="glass-effect rounded-2xl p-6 text-center max-w-md mx-auto mb-12">
          <div class="text-3xl mb-3">👤</div>
          <h3 class="font-bold text-white text-xl mb-2"><%= @participation.user.full_name %></h3>
          <p class="text-gray-400 mb-2"><%= pluralize(@participation.seats, 'place') %></p>
          <p class="text-gray-500 text-sm">
            Billet généré le <%= I18n.l(@participation.created_at, format: :short) %>
          </p>
        </div>

        <!-- Instructions avec icônes -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8 max-w-4xl mx-auto">
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-gold-500 to-gold-600 rounded-2xl flex items-center justify-center text-white text-2xl mx-auto mb-4">
              ⏰
            </div>
            <h3 class="font-bold text-white mb-2">Arrivée</h3>
            <p class="text-gray-400 text-sm">15 min avant</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center text-white text-2xl mx-auto mb-4">
              📱
            </div>
            <h3 class="font-bold text-white mb-2">Scanner</h3>
            <p class="text-gray-400 text-sm">QR Code à l'entrée</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl flex items-center justify-center text-white text-2xl mx-auto mb-4">
              🆔
            </div>
            <h3 class="font-bold text-white mb-2">Identité</h3>
            <p class="text-gray-400 text-sm">Si demandée</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-600 rounded-2xl flex items-center justify-center text-white text-2xl mx-auto mb-4">
              🎬
            </div>
            <h3 class="font-bold text-white mb-2">Profitez</h3>
            <p class="text-gray-400 text-sm">De l'expérience</p>
          </div>
        </div>

      </div>
    </div>
  </section>
</div>