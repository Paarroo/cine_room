<div id="event-detail" class="mt-24">
  <section class="py-8 min-h-screen">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Breadcrumb -->
      <nav class="flex items-center space-x-2 text-sm text-gray-400 mb-8">
        <%= link_to "Accueil", root_path, class: "hover:text-gold-500 transition-colors" %>
        <i class="fas fa-chevron-right text-xs"></i>
        <%= link_to "Événements", events_path, class: "hover:text-gold-500 transition-colors" %>
        <i class="fas fa-chevron-right text-xs"></i>
        <span class="text-white">Confirmation</span>
      </nav>

      <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">

        <!-- Main -->
        <div class="xl:col-span-2 space-y-8">

          <!-- Header -->
          <div class="glass-effect rounded-3xl overflow-hidden">
            <div class="relative h-96 bg-gradient-to-br from-red-500/20 via-pink-500/20 to-purple-500/20">
              <div class="absolute inset-0 bg-gradient-to-t from-dark-400 via-dark-400/60 to-transparent"></div>
              <div class="absolute bottom-6 left-6 right-6">
                <h1 class="text-4xl lg:text-6xl font-bold mb-4 gradient-text"><%= @event.title %></h1>
                <p class="text-xl text-gray-300 mb-6">Réalisé par <%= @event.movie.director %></p>
                <div class="flex flex-wrap items-center gap-6 text-gray-300">
                  <div class="flex items-center"><i class="fas fa-calendar mr-2 text-gold-500"></i><%= l(@event.event_date, format: :long) %></div>
                  <div class="flex items-center"><i class="fas fa-clock mr-2 text-gold-500"></i><%= @event.start_time.strftime("%Hh%M") %></div>
                  <div class="flex items-center"><i class="fas fa-map-marker-alt mr-2 text-gold-500"></i><%= @event.venue_name %></div>
                  <div class="flex items-center"><i class="fas fa-users mr-2 text-gold-500"></i><%= @event.participations.count %>/<%= @event.max_capacity %> places</div>
                </div>
              </div>
            </div>

            <!-- Confirmation -->
            <div class="p-8 space-y-6">
               <h2 class="text-4xl font-bold mb-6 gradient-text">🎉 Merci pour ta réservation !</h2>
              <p class="text-gray-300">Ta réservation a bien été enregistrée. Un e-mail de confirmation t’a été envoyé.</p>
              <ul class="prose prose-lg text-gray-300">
                <li><strong>Participation n° :</strong> <%= @participation.id %></li>
                <li><strong>Places réservées :</strong> <%= @participation.seats %></li>
                <li><strong>Durée :</strong> <%= @event.movie.duration %> minutes</li>
                <li><strong>Langue :</strong> <%= @event.movie.language.upcase %></li>
                <li><strong>Année :</strong> <%= @event.movie.year %></li>
              </ul>
            </div>
          </div>

          <!-- Metadata -->
          <div class="glass-effect rounded-3xl p-8">
            <div class="grid grid-cols-2 lg:grid-cols-4 gap-6 text-center">
              <div>
                <div class="text-2xl font-bold gradient-text mb-1">4.8</div>
                <div class="text-sm text-gray-400">Note spectateurs</div>
                <div class="flex justify-center mt-1 text-yellow-400">
                  <i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i>
                </div>
              </div>
              <div>
                <div class="text-2xl font-bold gradient-text mb-1"><%= @event.movie.duration %></div>
                <div class="text-sm text-gray-400">Minutes</div>
              </div>
              <div>
                <div class="text-2xl font-bold gradient-text mb-1"><%= @event.movie.year %></div>
                <div class="text-sm text-gray-400">Année</div>
              </div>
              <div>
                <div class="text-2xl font-bold gradient-text mb-1"><%= @event.movie.language.upcase %></div>
                <div class="text-sm text-gray-400">Langue</div>
              </div>
            </div>
          </div>

          <!-- Synopsis -->
          <div class="glass-effect rounded-3xl p-8">
            <h2 class="text-3xl font-bold mb-6">Synopsis</h2>
            <p class="text-gray-300 leading-relaxed"><%= @event.movie.synopsis %></p>
          </div>

          <!-- Team -->
          <div class="glass-effect rounded-3xl p-8">
            <h2 class="text-3xl font-bold mb-6">Équipe présente ce soir</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div class="flex items-center space-x-4 p-4 bg-white/5 rounded-2xl">
                <div class="w-16 h-16 bg-gradient-to-br from-gold-500 to-gold-600 rounded-2xl flex items-center justify-center text-white font-bold text-xl">
                  CM
                </div>
                <div>
                  <h3 class="text-xl font-bold"><%= @event.movie.director %></h3>
                  <p class="text-gray-400">Réalisateur·rice</p>
                </div>
              </div>
            </div>
          </div>

        </div>

        <!-- Sidebar -->
        <div class="xl:col-span-1">
          <div class="sticky top-24 space-y-6">


            <!-- Dashboard CTA -->
            <div class="glass-effect rounded-3xl p-6">
              <h3 class="text-xl font-bold mb-4">
                <i class="fas fa-ticket-alt text-gold-500 mr-2"></i> Gérer mes réservations
              </h3>
              <%= link_to "Voir mes événements", users_dashboard_path(current_user),
                class: "block w-full text-center bg-gradient-to-r from-yellow-500 to-yellow-600 hover:from-yellow-600 hover:to-yellow-700 text-white font-bold py-3 rounded-xl text-lg transition-all duration-300 hover:scale-105" %>
            </div>

            <!-- Lieu -->
            <div class="glass-effect rounded-3xl p-6">
              <h3 class="text-xl font-bold mb-4">
                <i class="fas fa-map-marker-alt text-gold-500 mr-2"></i>Lieu de l'événement
              </h3>
              <h4 class="font-bold text-white"><%= @event.venue_name %></h4>
              <p class="text-gray-400 text-sm"><%= @event.venue_address %></p>

              <div class="h-40 bg-white/5 rounded-xl overflow-hidden relative mt-4">
                <div class="absolute inset-0 flex items-center justify-center text-center text-gray-400">
                  <i class="fas fa-map text-3xl mb-2"></i>
                  <p class="text-sm">Carte interactive</p>
                </div>
              </div>

              <div class="space-y-2 text-sm mt-4">
                <div class="flex items-center text-gray-400"><i class="fas fa-subway mr-3 text-gold-500"></i>Métro Saint-Paul (ligne 1)</div>
                <div class="flex items-center text-gray-400"><i class="fas fa-parking mr-3 text-gold-500"></i>Parking public rue de Rivoli</div>
              </div>
            </div>

          </div>
        </div>

      </div>
    </div>
  </section>
</div>
