<nav class="fixed top-0 w-full z-50 nav-blur border-b border-white/10">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <%= link_to root_path, class: "flex items-center space-x-3 cursor-pointer" do %>
        <div class="w-10 h-10 bg-gradient-to-br from-gold-500 to-gold-600 rounded-xl flex items-center justify-center">
          <i class="fas fa-film text-white text-lg"></i>
        </div>
        <div class="flex flex-col">
          <span class="text-xl font-bold tracking-tight">CinéRoom</span>
          <span class="text-xs text-gray-400 uppercase tracking-widest">Cinéma Indépendant</span>
        </div>
      <% end %>

      <!-- Navigation Desktop -->
      <div class="hidden lg:flex items-center space-x-8">
        <button class="nav-btn text-gray-300 hover:text-gold-500 transition-colors duration-200 font-medium" data-target="home">Accueil</button>
        <button class="nav-btn text-gray-300 hover:text-gold-500 transition-colors duration-200 font-medium" data-target="creators">Créateurs</button>
        <button class="nav-btn text-gray-300 hover:text-cinema-blue transition-colors duration-200 font-medium" data-target="venues">Lieux</button>
        <button class="nav-btn text-gray-300 hover:text-gold-500 transition-colors duration-200 font-medium" data-target="events">Événements</button>
      </div>

      <!-- User Actions -->
      <div class="hidden lg:flex items-center space-x-4">
        <%= link_to "Connexion", new_user_session_path, class: "nav-btn px-4 py-2 text-gray-300 hover:text-white transition-colors duration-200 font-medium" %>
        <%= link_to "Rejoindre", new_user_registration_path, class: "nav-btn px-6 py-2 bg-gradient-to-r from-gold-500 to-gold-600 hover:from-gold-600 hover:to-gold-700 text-white rounded-full font-medium transition-all duration-200 hover:scale-105" %>
      </div>

      <!-- Mobile Menu Button -->
      <button class="lg:hidden w-10 h-10 bg-white/10 rounded-full flex items-center justify-center" id="mobile-toggle">
        <i class="fas fa-bars text-gray-300"></i>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="lg:hidden hidden nav-blur border-t border-white/10">
    <div class="px-4 py-6 space-y-4">
      <button class="nav-btn block w-full text-left py-3 text-gray-300 hover:text-gold-500 transition-colors" data-target="home">Accueil</button>
      <button class="nav-btn block w-full text-left py-3 text-gray-300 hover:text-gold-500 transition-colors" data-target="creators">Créateurs</button>
      <button class="nav-btn block w-full text-left py-3 text-gray-300 hover:text-cinema-blue transition-colors" data-target="venues">Lieux</button>
      <button class="nav-btn block w-full text-left py-3 text-gray-300 hover:text-gold-500 transition-colors" data-target="events">Événements</button>
    </div>
  </div>
</nav>
