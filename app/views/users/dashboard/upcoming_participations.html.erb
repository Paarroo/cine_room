<div class="page-content active mt-24">
  <section class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <header class="mb-12 text-center">
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-2">Mes participations à venir</h1>
      <p class="text-gray-400 text-sm md:text-base">Retrouve tous les événements auxquels tu es inscrit(e).</p>
    </header>

    <% if @upcoming_participations.any? %>
      <div class="space-y-6">
        <% @upcoming_participations.each do |p| %>
          <div class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-2xl shadow-sm hover:shadow-md transition duration-200">
            <div class="flex justify-between items-center flex-wrap gap-4">
              <div>
                <h2 class="text-lg font-semibold text-white mb-1"><%= p.event.title %></h2>
                <p class="text-sm text-gray-400">
                  <span class="block">📅 <%= l(p.event.event_date, format: :long) %></span>
                  <span class="block">📍 <%= p.event.venue_name %></span>
                </p>
              </div>
              <%= link_to "Voir l’événement", event_path(p.event), class: "text-sm text-yellow-400 hover:text-yellow-300 transition" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="text-center py-24">
        <p class="text-gray-400 text-lg">Tu n’as encore aucune participation à venir.</p>
      </div>
    <% end %>
  </section>
</div>
